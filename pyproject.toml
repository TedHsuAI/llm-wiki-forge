[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-wiki-forge"
version = "0.1.0"
description = "Skills and a CLI for building and querying LLM Wiki artifacts from .NET, Android, and iOS repositories."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "LLM Wiki Forge contributors" }
]
keywords = ["llm", "wiki", "csharp", "dotnet", "android", "ios", "swift", "objective-c", "ai-skills", "code-intelligence"]
dependencies = [
  "graphifyy>=0.4.10,<0.9",
  "langgraph>=0.2",
  "tree-sitter>=0.21",
  "tree-sitter-c-sharp>=0.21",
  "tree-sitter-kotlin>=1.0",
  "tree-sitter-java>=0.21",
  "tree-sitter-swift>=0.7.3",
  "tree-sitter-objc>=3.0.2",
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Topic :: Software Development :: Documentation",
]

[project.scripts]
llm-wiki = "llm_wiki_forge.cli:main"

[project.optional-dependencies]
graphrag = [
  "graphrag>=3.0,<4",
]

[tool.setuptools.packages.find]
include = ["llm_wiki_forge*"]

[tool.setuptools.package-data]
llm_wiki_forge = [
  "resources/*.py",
  "resources/scripts/*.py",
  "resources/scripts/query_runtime/*.py",
  "integrations/hermes/*.md",
  "integrations/hermes/*.json",
  "integrations/hermes/tools/*.py",
  "integrations/hermes/skills/llm-wiki-query/*.md",
  "integrations/hermes/hooks/*.py",
  "integrations/hermes/tests/*.py",
]
